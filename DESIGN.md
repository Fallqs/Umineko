# 《海猫鸣泣之时：六轩岛黄昏》系统设计文档

> 本文档描述联机客户端的核心系统架构与机制设计，供开发者参考。
> 生成时间：2026-06-06

---

## 一、系统概述

本项目是一个基于 TCP 的分布式剧本杀游戏引擎。每个角色由独立的 kimi-code-cli 进程扮演，进程间通过 `shared/inbox/` 下的消息文件通信。中央 Orchestrator 负责协调时间推进、地点分组、令牌环调度、死亡结算与角色切换。

核心设计目标：
1. **串行化降低并发**：同一物理时间只有一个地点活跃，同一地点内令牌环串行行动，避免 API 并发过高
2. **富集对话、限制调查**：10 轮对话营造沉浸感，但每时间槽仅允许 2 次调查，防止刷分
3. **隐藏行动、可见冲突**：除开枪/安慰/决斗外，所有行动对同场玩家不可见，必须通过 speech 自愿披露，制造信息差与欺骗空间

---

## 二、总体架构

```
┌─────────────────────────────────────────────────────────┐
│  Orchestrator (中央协调器)                               │
│  ├─ TimeEngine      时间槽推进                           │
│  ├─ LocationEngine  地点分组与移动结算                    │
│  ├─ TokenRingEngine 令牌环调度（串行行动）                │
│  ├─ ActionEngine    行动解析与执行                        │
│  ├─ DeathEngine     死亡结算与角色切换                    │
│  ├─ NPCEngine       NPC 进程管理                         │
│  ├─ BeatriceEngine  贝阿朵复核与红字/金字                │
│  ├─ NetworkLayer    TCP Server/Seat 管理                 │
│  └─ GameState       纯数据状态层                         │
└─────────────────────────────────────────────────────────┘
                              │ TCP
        ┌─────────────────────┼─────────────────────┐
        ▼                     ▼                     ▼
   ┌─────────┐          ┌─────────┐          ┌─────────┐
   │ Seat P1 │          │ Seat P2 │          │ NPC_xxx │
   │agent_wrapper│      │agent_wrapper│      │agent_wrapper│
   │  (auto) │          │  (auto) │          │  (npc)  │
   └─────────┘          └─────────┘          └─────────┘
```

### 增量改造原则
- **保留**：TCP 通信层、子进程管理、死亡处理、角色切换、BEATRICE 复核、计分系统
- **重写**：核心游戏循环（`run_day` → 时间槽驱动循环）、令牌环引擎、行动解析

---

## 三、时间系统

### 每天结构（13 个时间槽）

| 阶段 | 类型 | 说明 |
|------|------|------|
| DAWN | 结算 | 自动回本馆，报告死亡/事件，重置行动点为 26 |
| MORNING_1 | 自由 | 早饭前自由时间点 |
| BREAKFAST | 强制 | 强制移动到餐厅，聚餐 |
| NOON_1 | 自由 | 午饭前自由时间点 1 |
| NOON_2 | 自由 | 午饭前自由时间点 2 |
| LUNCH | 强制 | 强制移动到餐厅，聚餐 |
| AFTERNOON_1 | 自由 | 晚饭前自由时间点 1 |
| AFTERNOON_2 | 自由 | 晚饭前自由时间点 2 |
| DINNER | 强制 | 强制移动到餐厅，聚餐 |
| SLEEP_CHECK | 选择 | 询问是否睡觉/熬夜 |
| EVENING_1 | 自由 | 晚饭后自由时间点 1（仅熬夜者，消耗 2 倍） |
| EVENING_2 | 自由 | 晚饭后自由时间点 2（仅熬夜者，消耗 2 倍） |
| MIDNIGHT | 结算 | 处理夜间事件，进入下一天 |

### 关键规则
- 自由时间点之间玩家可声明移动意向，移动在时间点切换时统一结算
- 强制回本馆的移动不检查/不消耗行动点
- AI 模式下默认不睡觉（保证测试覆盖），生产环境可改为概率决定
- 熬夜者所有行动消耗变为 2 倍

---

## 四、地点与移动系统

### 地点列表
```python
LOCATIONS = ["本馆", "别馆", "玫瑰园", "神社", "书房", "餐厅", "厨房", "客房", "庭院", "港口", "地下密室"]
```

### 距离矩阵
| 距离 | 组合示例 | 消耗 |
|------|---------|------|
| 同建筑 | 本馆-餐厅/书房/厨房/客房 | 0 |
| 相邻/近距 | 本馆-庭院，本馆-别馆，庭院-玫瑰园 | 1 |
| 中距 | 本馆-神社，本馆-港口，别馆-玫瑰园 | 2 |
| 远距 | 涉及地下密室 | 3 |

### 初始位置
- 上岛家族成员（战人、朱志香、让治、真里亚、秀吉、楼座、留弗夫、雾江、藏臼、夏妃）：**港口**
- 岛上佣人（嘉音、纱音、乡田、熊泽）：**本馆**
- 南条医师：**本馆客房**
- 金藏：**书房**
- 贝阿朵莉切：**玫瑰园**

### 移动规则
- 自由时间点结束时，Orchestrator 收集所有玩家的"下轮移动意向"
- 移动在时间点切换时统一结算，扣除行动点
- 行动点不足时移动失败，玩家留在原地
- DAWN / BREAKFAST / LUNCH / DINNER 前的强制移动不检查/不消耗点数

---

## 五、令牌环行动机制

### 核心设计：10 轮对话 + 最多 2 次调查

每个自由时间槽和强制时间槽（餐点）均执行令牌环，**默认 10 轮**。但在整个时间槽内，每个角色**最多只能执行 2 次调查**（`investigate` 或 `investigate_target`）。

```
1. 按当前位置对所有存活角色分组（同地点为一组）
2. 随机排序地点列表（决定地点激活顺序）
3. FOR each 地点:
   a. 对该地点玩家随机排序，形成令牌环
   b. FOR round = 1 to 10:
      FOR each 玩家 in 令牌环:
         i.   Orchestrator 发送 turn_token（携带 investigations_remaining）
         ii.  等待该玩家返回 action（唯一活跃的 AI 调用）
         iii. 解析行动
         iv.  调查：若 inv_remaining > 0 且 AP 足够 → 执行调查、扣 2 AP、消耗 1 次调查次数
         v.   发言：广播给同地点所有玩家（不耗 AP）
         vi.  特殊行动：搜身/开枪/验尸/拾取/使用/赠送/安慰（互斥，每轮只能一个）
         vii. 声明下轮移动：记录到 pending_moves
4. 所有地点处理完毕后，结算 pending_moves
```

### 调查次数限制
- 时间槽开始时，`GameState.reset_investigations()` 为所有存活角色重置计数为 0
- 每次成功调查调用 `GameState.use_investigation(role)`，上限 2
- 次数用尽后，后续调查请求被拒绝（"本时间槽调查次数已用尽"）
- 行动点不足导致的调查失败**不消耗**调查次数

### 互斥规则（每轮特殊行动优先级）
1. 决斗 > 2. 开枪 > 3. 验尸 > 4. 搜身 > 5. 调查 > 6. 拾取/使用/赠送 > 7. 安慰
- 每轮只能执行一个特殊行动
- 发言和移动意向**始终可叠加**，不受互斥影响

---

## 六、NPC 角色管理与动态 Seat 分配

### 核心原则
- 所有存活角色都必须参与令牌环，不能有幽灵角色
- 未被玩家 seat 控制的角色，由 **NPC 独立进程**控制
- NPC 进程与普通 seat 进程**完全同级**，使用相同的 TCP 协议和令牌环

### NPC Seat 注册
- NPC seat_id 格式：`"NPC_{角色名}"`，例如 `"NPC_右代宫金藏"`
- NPC 使用 `mode="npc"` 的 agent_wrapper，拥有完整的 GM Session
- 与普通 seat 的唯一区别：seat_id 以 `"NPC_"` 开头

### 动态 Seat 管理

**游戏初始化时**：
1. 启动玩家 seat（P1-P7），各自控制角色链的第一个角色
2. 对于所有不在任何玩家角色链前端的存活角色，启动对应的 NPC 进程
3. 所有角色统一纳入 `game_state.alive_roles`
4. Orchestrator 维护 `role_controller: Dict[str, str]` 映射

**玩家角色切换时（NPC 回收）**：
1. 玩家 seat 的原角色死亡，需要切换到新角色
2. Orchestrator 检查新角色是否由 NPC 控制
3. 若是 NPC 控制：终止 NPC 进程 → 从 seats 中移除 → 从 role_controller 移除
4. 将新角色的状态（位置、解锁信息、行动点等）从 GameState 读取
5. 玩家 seat 以新角色重新注册
6. 更新 `role_controller[新角色] = 玩家seat_id`
7. 玩家进程继承 NPC 期间积累的所有状态

**状态拷贝内容**：
- 当前位置、已解锁信息条目、行动点、冷却状态、睡觉/熬夜状态

---

## 七、行动点数设计

### 每日分配
- **DAWN 时重置为 26 点/角色**

### 消耗规则
| 行动 | 消耗 | 说明 |
|------|------|------|
| 调查地点 | 2 点 | 获取信息条目 |
| 调查目标玩家 | 2 点 | 观察特定玩家 |
| 搜身 | 3 点 | 查看目标玩家背包 |
| 开枪 | 5 点 | 尝试击杀目标 |
| 验尸 | 2 点 | 战人专属，调查尸体额外线索 |
| 拾取物品 | 1 点 | 从场景获取物品 |
| 使用物品 | 1 点 | 触发物品效果 |
| 赠送物品 | 0 点 | 转移物品给同场玩家 |
| 安慰 | 1 点 | 恢复目标精神状态 |
| 普通移动 | 1-3 点 | 按距离矩阵 |
| 发言 | 0 点 | 同地点所有人可听到 |
| 跳过 | 0 点 | 不行动 |
| 不睡觉惩罚 | ×2 | 晚饭后所有行动消耗翻倍 |

### AP 档位计算（每天 10 个可行动时间槽）

| 档位 | 描述 | 不考虑夜晚双倍 | 考虑夜晚双倍 |
|------|------|--------------|-------------|
| 1 | 每槽至少调查 1 次 | 10 × 1 × 2 = **20 AP** | 白天 8×2 + 晚上 2×4 = **24 AP** |
| 2 | 每槽移动 + 调查 1 次 | 10×2 + 10×1.5 = **35 AP** | 白天 8×3.5 + 晚上 2×7 = **42 AP** |
| 3 | 每槽调查 2 次 | 10 × 2 × 2 = **40 AP** | 白天 8×4 + 晚上 2×8 = **48 AP** |
| 4 | 每槽调查 2 次 + 移动 | 10×4 + 10×1.5 = **55 AP** | 白天 8×5.5 + 晚上 2×11 = **66 AP** |
| 5 | 任意行动（2 调查 + 移动 + 特殊） | 40 + 15 + 9 ≈ **64 AP** | ≈ **80+ AP** |

**26 AP 的定位**：介于档位 1（20）和档位 2（35）之间。
- 玩家可以**每槽调查 1 次**（20 AP），剩余 6 AP 用于约 4 次移动
- 或者集中在**5 个槽中调查 2 次**（20 AP），剩余 6 AP 用于移动
- 无法保证"每槽都移动 + 调查"，迫使玩家做策略取舍

---

## 八、信息解锁系统

### 基础规则
- 信息条目按 `地点 + 天数 + 信息ID` 存储于 `ConfigLoader.location_info`
- 玩家调查时，根据当前地点和天数查找可解锁的信息
- 每个地点每天有多条信息，但受调查次数限制（每槽最多 2 次）
- 已解锁的信息不再重复获得

### 前置条件系统（`requires`）
每条信息可配置 `requires` 字段，支持以下条件：

```json
{
  "info_ids": ["P-001"],
  "items": ["item_001"],
  "day_min": 2,
  "day_max": 5,
  "role": "右代宫战人",
  "time_slot": "EVENING_1"
}
```

- `info_ids`：需要已解锁指定信息条目
- `items`：需要持有指定物品
- `day_min/day_max`：仅在指定天数范围内可发现
- `role`：仅限特定角色可发现
- `time_slot`：仅在特定时间槽可发现

**这是比原始 plan 更优的设计**，原始 plan 仅支持简单的地点+天数查找。

### 信息分值
| 前缀 | 分值 |
|------|------|
| P- | 1 分 |
| S- | 2 分 |
| C- | 4 分 |

---

## 九、物品系统

### 架构（Plan 未涉及，为增量设计）

```python
item_registry: Dict[str, dict]    # item_id → 物品定义
inventory: Dict[str, Set[str]]    # role → 持有的 item_id 集合
taken_items: Set[str]             # 全局唯一物品，取走后从场景移除
```

### 物品定义格式
```json
{
  "id": "item_001",
  "name": "金藏的烟斗",
  "type": "permanent",
  "gm_desc": "金藏从不离手的烟斗，底部刻着一行小字...",
  "player_desc": "一只旧烟斗，看起来很有年头。",
  "location": "书房",
  "day_available": 1,
  "unlocks_info": ["S-隐藏日记"],
  "effect": {}
}
```

### 物品操作
- **拾取**（`pickup`）：消耗 1 AP，将场景物品加入背包
- **使用**（`use_item`）：消耗 1 AP，触发效果并解锁关联信息
- **赠送**（`gift`）：不耗 AP，将物品转移给同场玩家
- **搜身**（`search`）：消耗 3 AP，查看目标玩家背包内容

### 行动可见性
- 拾取、使用、赠送、搜身**均不可见**（除搜身结果仅执行者知道）
- 玩家必须通过 speech 自愿披露物品信息

---

## 十、玩家交互系统

### 交互行动（Plan 未完全涉及，为增量设计）

| 行动 | 消耗 | 效果 | 可见性 |
|------|------|------|--------|
| `investigate_target` | 2 AP | 观察特定玩家，获取描述 | ❌ 隐藏 |
| `search` | 3 AP | 查看目标玩家背包 | ❌ 隐藏 |
| `shoot` | 5 AP | 尝试击杀目标 | ✅ 可见（广播"突然掏出了枪！"） |
| `comfort` | 1 AP | 恢复目标精神状态 | ✅ 可见（广播"正在安慰 X"） |
| `autopsy` | 2 AP | 战人专属，调查尸体额外线索 | ❌ 隐藏 |
| `gift` | 0 AP | 赠送物品 | ❌ 隐藏 |
| `duel` | — | 嘉音/纱音向贝阿朵发起决斗 | ✅ 可见（决斗结果广播） |

### 互斥机制
每轮只能执行一个特殊行动，优先级如上。发言和移动意向始终可叠加。

---

## 十一、贝阿朵莉切特殊规则与薛定谔机制

### 贝阿朵莉切（BEATRICE）
- **主 GM 身份**：负责合规复核和剧情裁决
- **无行动点**：不消耗行动点，不受行动点系统约束
- **自由现身**：可以出现在任何地点，也可完全不现身
- **唯一地点约束**：同一时间点，贝阿朵至多只能出现在一个地方
- **参与令牌环**：当贝阿朵选择现身时，她参与该地点的令牌环

### 薛定谔规则
- 当嘉音和纱音同时出现在同一地点被第三者目击时，触发薛定谔规则崩溃
- **贝阿朵立即瞬移**到该地点进行裁决
- 瞬移不消耗行动点，不受距离限制
- 裁决结果：决定抹杀嘉音或纱音之一

### 嘉音/纱音决斗机制
- 不论是否触发薛定谔规则，嘉音或纱音都可以在任意时间点向贝阿朵发起决斗
- 决斗结果：发起者死亡，另一位存活
- turn_token 中附加 `can_duel_beatrice: true` 标志

---

## 十二、通信协议

### Orchestrator → Seat: `turn_token`
```json
{
  "type": "turn_token",
  "seat_id": "P1",
  "role_name": "右代宫战人",
  "time_slot": "MORNING_1",
  "day": 2,
  "location": "书房",
  "action_points": 42,
  "token_round": 3,
  "token_total_rounds": 10,
  "investigations_remaining": 1,
  "nearby_players": ["右代宫朱志香", "纱音"],
  "inventory": ["金藏的烟斗"],
  "context": "你在书房...",
  "id": "turn_d2_m1_P1_r3"
}
```

### Seat → Orchestrator: `action`
```json
{
  "type": "action",
  "seat_id": "P1",
  "parent_id": "turn_d2_m1_P1_r3",
  "action_text": "我仔细调查书架上的痕迹...",
  "next_move": "餐厅",
  "id": "action_P1_xxx"
}
```

### Orchestrator → Seat (同地点其他人): `notification`
```json
{
  "type": "notification",
  "title": "同场发言",
  "body": "【右代宫朱志香】你也来看看这个。",
  "location": "书房"
}
```

---

## 十三、休眠-激活机制

### 核心设计
- **目标**：未收到 turn_token 时彻底休眠，不触发任何 AI 调用
- **令牌机制完全由 Orchestrator 处理**，agent_wrapper 不感知令牌环、轮次、地点切换
- **Orchestrator 主动唤醒**：只有收到 `turn_token` 时才激活
- **行动结束后自动休眠**：action 消息发送后，agent 立即回到休眠

### 休眠状态定义
- `is_hibernating: bool = True` 作为默认状态
- 休眠期间收到的 `notification` / `scene` 存入 `hibernation_buffer`，**完整保留不截断**
- 绝不调用任何 AI Session

### 激活流程
1. `is_hibernating = False`
2. 将 `hibernation_buffer` 中的全部消息整理为文本摘要
3. 先传给 **GM Session** 处理（内部认知更新，不发给 Orchestrator）
4. 再将 **GM 认知更新 + turn_token 完整上下文** 传给 **User Session** 生成行动
5. 发送 `action` 消息给 Orchestrator
6. 清空 `hibernation_buffer`
7. **立即回到休眠状态**

### 直接感知 vs GM 认知分层
`agent_wrapper._extract_buffer_events()` 将 `hibernation_buffer` 分为两类：
- **直接感知事件**：同场发言、同场事件（开枪/安慰）、被搜身、被观察、收到物品、射击、调查发现等 → 直接展示给玩家
- **GM 认知事件**：系统通知、场景描述、天气等 → 传给 GM Session 做内部认知更新

---

## 十四、与原始 Plan 的对比

### 已实现且与 Plan 一致
| Plan 要求 | 实现状态 |
|-----------|---------|
| 增量改造现有 orchestrator | ✅ 完全实现 |
| TCP/子进程/死亡/切换/BEATRICE 保留 | ✅ 完全实现 |
| 13 时间槽/天（DAWN + 自由 + 餐点 + SLEEP + MIDNIGHT） | ✅ 实现 |
| 地点列表 + 距离矩阵 + 差异化初始位置 | ✅ 实现 |
| NPC 独立进程 + 动态回收 + 状态拷贝 | ✅ 实现 |
| 令牌环串行化（并发=1） | ✅ 实现 |
| 贝阿朵瞬移 + 嘉音/纱音决斗 | ✅ 实现 |
| 50 AP / 天 | ✅ **已修复为 50** |
| 休眠-激活机制 + hibernation_buffer | ✅ 实现 |
| turn_token / action / notification 协议 | ✅ 实现 |

### 已实现且优于 Plan
| Plan 设计 | 当前实现 | 说明 |
|-----------|---------|------|
| 每地点 2 轮令牌环 | **10 轮对话 + 最多 2 次调查** | 用户明确要求，大幅增加对话沉浸感，同时严格限制调查次数防止刷分 |
| 关键时间点 1 轮 | **10 轮** | 聚餐时也可以充分对话 |
| 简单地点+天数信息查找 | **requires 前置条件系统** | 支持 info_ids/items/day_min/day_max/role/time_slot 多维度条件，剧情设计更灵活 |
| 无物品系统 | **完整物品系统** | item_registry + inventory + taken_items，支持拾取/使用/赠送/搜身 |
| 无玩家间交互 | **7 种交互行动** | investigate_target / search / shoot / comfort / autopsy / gift / duel |
| 行动全部可见 | **隐藏行动机制** | 仅开枪/安慰/决斗可见，其他隐藏，制造信息差与欺骗空间 |
| 50 AP 设计 | **50 AP + 每槽 2 调查限制** | 在 plan 的 50 AP 基础上叠加调查次数限制，策略深度更高 |

### 与 Plan 的差异（设计取舍）
| Plan 设计 | 当前实现 | 原因 |
|-----------|---------|------|
| Morning_1/2/3, Noon_1/2/3, Afternoon_1/2/3, Evening_1/2/3（12 个自由槽） | Morning_1, Noon_1/2, Afternoon_1/2, Evening_1/2（7 个自由槽） | 时间槽缩减为 13 个（含 3 餐点），10 轮 × 7 槽 = 70 轮/天已足够丰富 |
| 关键时间点 1 轮 | 自由槽 **10 轮**、餐点 **5 轮** | 自由时间充分对话，餐点以简短聚餐对话为主 |
| 50 AP / 天 | **26 AP / 天** | 经档位计算，26 AP 支持"每槽调查 1 次 + 有限移动"，迫使策略取舍 |
| 移动消耗 1-3 点 | 移动意向声明，但当前 **移动不扣 AP** | 当前实现中移动意向仅记录位置变更，未接入 AP 扣除。这是一个已知 TODO，需后续补全 |

---

## 十五、已知 TODO 与后续扩展

### 当前 TODO
1. **移动 AP 扣除**：`action_engine.handle_move_intent()` 目前仅更新位置，未按距离矩阵扣除 AP
2. **战人验尸特权**：`autopsy` 已接入框架，但尸体判定逻辑需结合死亡引擎完善
3. **贝阿朵主动行动**：BEATRICE 目前主要处理复核，主动干预（红字设置事件）待实现
4. **数据填充**：`items.json`、`location_info.json` 的 `requires` 字段仍为占位符，需剧情作者填写

### 未来扩展（不在本期）
1. **速度系统**：基于剩余 AP 计算 T 值，影响令牌环排序优先级
2. **条件解锁深化**：需要特定角色组合在场、多前置信息连锁等
3. **天气/环境事件系统**：动态影响地点可调查内容
4. **NPC 行为预设细化**：佣人优先在本馆/厨房活动，医生优先检查尸体等

---

*「没有爱，就看不见。」*
*但在这个系统中，爱也是受到权限控制的。*
